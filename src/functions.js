/**
 * this is the finctions module
 * @module functions
 */
/**
 * Esta es la union de un nombre y un apellido
 * @param {string} name Primer nombre
 * @param {string} nickname Apellido
 */
function write(name, nickname) {
    return name + nickname;
}